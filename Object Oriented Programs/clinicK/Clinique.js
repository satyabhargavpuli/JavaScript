const utiltiy=require();
var read = require('readline-sync');
 const fs=require('fs')
 function clinique(params) {  
  var data=fs.readFileSync('cliniq.json','utf8');
 var object=JSON.parse(data);
 var obj=new utiltiy.clinq;

}